This folder contains a script to scrape public transport times from specified origins to destinations using selenium.

There is a google routes api (https://developers.google.com/maps), but this doesn't allow calculations public transport to be used as a transport mode.

